# @package _global_

defaults:
  - /benchmark/nav/socialnav_v2: falcon_hm3d_task_for_train
  - /habitat_baselines: habitat_baselines_rl_config_base
  - /habitat/simulator/sim_sensors@habitat_baselines.eval.extra_sim_sensors.third_rgb_sensor: third_rgb_sensor 
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_1: single_fixed
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_2: single_fixed
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_3: single_fixed
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_4: single_fixed
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_5: single_fixed
  - /habitat_baselines/rl/policy@habitat_baselines.rl.policy.agent_6: single_fixed
  - /habitat/task/actions@habitat.task.actions.agent_0_discrete_stop: discrete_stop
  - /habitat/task/actions@habitat.task.actions.agent_0_discrete_move_forward: discrete_move_forward
  - /habitat/task/actions@habitat.task.actions.agent_0_discrete_turn_left: discrete_turn_left
  - /habitat/task/actions@habitat.task.actions.agent_0_discrete_turn_right: discrete_turn_right
  - /habitat/task/actions@habitat.task.actions.agent_1_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/actions@habitat.task.actions.agent_2_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/actions@habitat.task.actions.agent_3_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/actions@habitat.task.actions.agent_4_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/actions@habitat.task.actions.agent_5_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/actions@habitat.task.actions.agent_6_oracle_nav_randcoord_action_obstacle: oracle_nav_action 
  - /habitat/task/lab_sensors@habitat.task.lab_sensors.agent_0_pointgoal_with_gps_compass: pointgoal_with_gps_compass_sensor
  - _self_

habitat:
  dataset:
    split: train
  gym:
    obs_keys: 
      # Adapt3R required sensors
      # - agent_0_articulated_agent_jaw_rgb         # RGB camera for Adapt3R
      - agent_0_articulated_agent_jaw_depth       # Depth camera for Adapt3R
      # - agent_0_jaw_intrinsics  # Camera intrinsics for Adapt3R
      # - agent_0_jaw_extrinsics  # Camera extrinsics for Adapt3R
      # Navigation sensors
      - agent_0_pointgoal_with_gps_compass
      - agent_0_localization_sensor
      - agent_0_human_num_sensor  
      - agent_0_oracle_humanoid_future_trajectory
  task:
    actions:
      agent_0_discrete_stop:
          lin_speed: 0.0 
          ang_speed: 0.0
      agent_0_discrete_move_forward:
        lin_speed: 25.0
        ang_speed: 0.0
        allow_dyn_slide: True
      agent_0_discrete_turn_left:
        lin_speed: 0.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_0_discrete_turn_right:
        lin_speed: 0.0
        ang_speed: -10.0
        allow_dyn_slide: True
      agent_1_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_2_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_3_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_4_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_5_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
      agent_6_oracle_nav_randcoord_action_obstacle:
        type: OracleNavRandCoordAction_Obstacle 
        motion_control: human_joints
        lin_speed: 10.0
        ang_speed: 10.0
        allow_dyn_slide: True
habitat_baselines:
  evaluate: False # True
  verbose: False 
  trainer_name: "falcon_trainer" ## original ppo works for self._agent, but we need to train self._agent._agents[0]
  torch_gpu_id: 0
  tensorboard_dir: "evaluation/falcon/hm3d/tb"
  video_dir: "evaluation/falcon/hm3d/video"
  test_episode_count: -1
  num_environments: 8
  checkpoint_folder: "evaluation/falcon/hm3d/checkpoints_87" # save_dir for training
  num_updates: -1
  total_num_steps: 15000000 # exact steps 12560368
  log_interval: 10
  num_checkpoints: 20 # interrupt recovery
  # Force PyTorch to be single threaded as
  # this improves performance considerably
  force_torch_single_threaded: True
  load_resume_state_config: False
  # train_ckpt: "evaluation/falcon/hm3d/checkpoints/latest.pth" # "evaluation/falcon/hm3d/checkpoints/ckpt.000020.pth"

  evaluator:
    _target_: habitat_baselines.rl.ppo.falcon_evaluator.FALCONEvaluator

  eval:
    use_ckpt_config: False 
    should_load_ckpt: True

  rl:
    agent:
      type: "MultiAgentAccessMgr"
      num_agent_types: 7
      num_active_agents_per_type: [1, 1, 1, 1, 1, 1, 1]
      num_pool_agents_per_type: [1, 1, 1, 1, 1, 1, 1]
      agent_sample_interval: 20 
      force_partner_sample_idx: -1
    policy:
      # Adapt3R policy for the main agent
      agent_0:
        name: "PointNavResNetPolicy"
        action_distribution_type: "categorical"

      # Other agents can use fixed or different policies
      agent_1:
        name: "FixedPolicy"
      agent_2:
        name: "FixedPolicy"
      agent_3:
        name: "FixedPolicy"
      agent_4:
        name: "FixedPolicy"
      agent_5:
        name: "FixedPolicy"
      agent_6:
        name: "FixedPolicy"
    ppo:
      # ppo params
      clip_param: 0.2
      ppo_epoch: 2
      num_mini_batch: 2
      value_loss_coef: 0.5
      entropy_coef: 0.01
      lr: 2.5e-5
      eps: 1e-5
      max_grad_norm: 0.2
      num_steps: 64
      use_gae: True
      gamma: 0.99
      tau: 0.95
      use_linear_clip_decay: False
      use_linear_lr_decay: False
      reward_window_size: 50

      use_normalized_advantage: False

      # Adapt3R network configuration
      hidden_size: 512

      # Use double buffered sampling, typically helps
      # when environment time is similar or larger than
      # policy inference time during rollout generation
      use_double_buffered_sampler: False

    ddppo:
      sync_frac: 0.6
      # The PyTorch distributed backend to use
      distrib_backend: NCCL
      
      pretrained_weights: /root/zjm/SocialNav2/outputs/il_model_best.pth
      # Adapt3R: No pretrained weights available
      pretrained: True
      pretrained_encoder: False  
      train_encoder: True
      # Whether to reset the critic linear layer
      reset_critic: True

      # Model parameters (overridden by Adapt3R policy config)
      backbone: resnet50  # Default, but Adapt3R uses its own backbone config
      rnn_type: LSTM       # Default, but Adapt3R uses its own RNN config  
      num_recurrent_layers: 2
      
      # Adapt3R-specific configuration
      adapt3r:
        visual_encoder:
          backbone_type: "resnet18"     # 使用 resnet18 更稳定，没有预训练模型
          hidden_dim: 126               # 必须能被6整除 (NeRF positional encoding)
          num_points: 256               # 点云采样数量
          do_image: True                # 使用视觉特征
          do_pos: True                  # 使用位置编码
          do_rgb: False                 # 不使用原始RGB
          finetune: True                # 训练视觉编码器 (没有预训练模型)
          xyz_proj_type: "nerf"         # NeRF 位置编码
          clip_model: "RN50"            # CLIP模型 (如果使用CLIP backbone)
          
          # 点云处理配置
          do_crop: True                 # 启用点云裁剪
          boundaries: [[-5.0, -5.0, 0.0], [5.0, 5.0, 3.0]]  # 裁剪边界 
          
          # 低维观测键 (导航相关传感器)
          lowdim_obs_keys: 
            - "pointgoal_with_gps_compass"
            - "localization_sensor"
      
    auxiliary_losses: 
      people_counting:
        max_human_num: 6
        loss_scale: 0.1   
      guess_human_position:
        max_human_num: 6
        position_dim: 2
        loss_scale: 0.1   
      future_trajectory_prediction:
        max_human_num: 6
        future_step: 4
        loss_scale: 0.1       